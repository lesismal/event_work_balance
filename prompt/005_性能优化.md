### 001

优化Go 版性能，epoll包、http包、websocket包，都要优化


### 002

在适合的情况下默认使用writev


### 003

根据pprof分析，然后继续优化性能


### 004

继续优化性能，比如多用sync Pool 管理高频对象。taskpool 把之前的参考nbio里的方式实现也放上，另外增加一个用cond_t的方式实现的，和当前实现的这种都保留，支持可配置使用哪个taskpool。